@page
@model ElderSense.Pages.Alertas.IndexModel

@{
    ViewData["Title"] = "Alertas";
}

<h1>Alertas</h1>

<table class="table">
    <thead>
        <tr>
            <th>Data/Hora</th>
            <th>Mensagem</th>
            <th>Idoso</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var alerta in Model.Alertas)
        {
            <tr>
                <td>@alerta.DataHora.ToString("dd/MM/yyyy HH:mm")</td>
                <td>@alerta.Mensagem</td>
                <td>@alerta.Utilizador.Nome</td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <script>
        // cria a ligação ao Hub do SignalR
        const connection = new signalR.HubConnectionBuilder()
            .withUrl("/alertaHub")
            .build();

        // define o que fazer quando o servidor enviar a mensagem "NovoAlerta"
        connection.on("NovoAlerta", function (mensagem) {
            alert("Novo alerta recebido: " + mensagem);
        });

        // inicia a ligação
        connection.start().catch(function (err) {
            console.error(err.toString());
        });
    </script>
}